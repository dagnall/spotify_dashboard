# Set default behavior to automatically normalize line endings.
* text=auto

# Explicitly declare text files you want to always be normalized and converted to native line endings on checkout.
*.py    text eol=lf
*.json  text eol=lf
*.md    text eol=lf

# Declare binary files to be left untouched by Git
*.png   binary
*.jpg   binary
*.gif   binary
*.ico   binary
*.svg   binary

# IDE and editor specific files
.vscode/ text=auto
.idea/ text=auto
*.sublime-project text=auto
*.sublime-workspace text=auto

# OS-specific files
Thumbs.db binary
.DS_Store binary
